# Current Archify Pointer

The active version is selected by the auditable manifest at `../manifests/archify.json`.
The wrapper reads the pinned commit from that manifest and never follows a floating branch.
